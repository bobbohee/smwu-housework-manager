rules_version = '2';

// Day 3에서 본격 작성. 현재는 에뮬레이터 기동을 위한 잠정 거부 규칙.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
